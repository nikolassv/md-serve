## 1. GitHub Actions Workflow

- [x] 1.1 Create `.github/workflows/ci.yml` with triggers for `push` to `main` and `pull_request`
- [x] 1.2 Configure the job to run on `ubuntu-latest`, set up Java 21 (Temurin), and execute `mvn verify`

## 2. Documentation and Changelog

- [x] 2.1 Update `CHANGELOG.md` under `[Unreleased]` to record the new CI workflow
- [x] 2.2 Update `docs/dev/architecture.md` to note CI/CD setup

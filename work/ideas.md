# Ideas

- if a directory contains an "index.md" redirect the user to this file instead of showing a directory listing
- correctly render latex math
- syntax highlighting for code blocks
- if the source directory is inside a git repository add a list of revisions of the current file to the template parameters. Give users the ability to open a specific revision by adding a revision parameter to the url
